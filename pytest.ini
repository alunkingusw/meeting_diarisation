[pytest]
testpaths = backend/tests
markers =
    slow: test is slow to run (real model downloads/inference, etc.)
    integration: test requires the full ML stack (whisper/pyannote/torch) and network/HF access
addopts = -m "not integration" --strict-markers
