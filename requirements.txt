fastapi
uvicorn
python-multipart
psycopg2-binary
sqlalchemy
python-dotenv
pydantic
pydantic-settings
bleach
werkzeug
alembic
python-jose[cryptography]

# Audio processing and transcription
openai-whisper
ffmpeg-python
pyannote.audio==3.3.2
# pyannote.audio 3.3.2 only declares huggingface_hub>=0.13.0 (no ceiling), and its
# Pipeline.from_pretrained()/Model.from_pretrained() calls still use the use_auth_token kwarg
# that huggingface_hub removed in 1.0 - pin below that until pyannote.audio itself moves to `token`.
huggingface_hub<1.0
scikit-learn
git+https://github.com/alunkingusw/brouhaha-vad.git@main
numpy

# Transcript chunking + retrieval (backend/transcript_rag)
# Keep the Chroma adapter and client on the same API generation as LangChain Core.
chromadb==1.5.9
langchain-core==1.6.3
langchain-chroma==0.2.6
sentence-transformers==3.1.1

# Meeting summarisation (backend/summarization) - local Ollama server client
httpx

# Transcript format conversion
pysubs2